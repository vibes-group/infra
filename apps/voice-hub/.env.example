# Заполняется автоматически workflow'ом из секретов. Этот файл — справка о
# содержимом /opt/vibes/voice-hub/.env на сервере.

IMAGE_TAG=<github commit sha>
APP_HOSTNAME=<voice-hub host>
PUBLIC_IP=<deploy host ipv4>
APP_ADMIN_PASSWORD=<admin pwd>
